import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:{{packageName}}/features/auth/domain/usecases/login_usecase.dart';
import 'login_notifier.dart';
import 'login_state.dart';

final loginProvider = StateNotifierProvider.autoDispose<LoginNotifier, LoginState>((ref) {
  return LoginNotifier(ref.watch(loginUseCaseProvider));
});

// Wire up your use case provider here — e.g. via a repository provider
final loginUseCaseProvider = Provider<LoginUseCase>((ref) {
  // TODO: inject AuthRepository from its own provider
  throw UnimplementedError('Provide AuthRepository');
});
