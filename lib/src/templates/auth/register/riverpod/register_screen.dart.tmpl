import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:{{packageName}}/core/utils/app_colors.dart';
import 'package:{{packageName}}/core/utils/app_text_styles.dart';
import 'package:{{packageName}}/core/utils/app_dimensions.dart';
import 'package:{{packageName}}/core/utils/app_strings.dart';
import 'package:{{packageName}}/core/constants/app_routes.dart';
import 'package:{{packageName}}/shared/widgets/primary_button.dart';
import 'package:{{packageName}}/shared/widgets/text_input.dart';
import 'package:{{packageName}}/features/auth/presentation/register/riverpod/register_provider.dart';

class RegisterScreen extends ConsumerWidget {
  const RegisterScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final state = ref.watch(registerProvider);
    final notifier = ref.read(registerProvider.notifier);

    ref.listen(registerProvider, (_, next) {
      if (next.isSuccess) Navigator.pushReplacementNamed(context, {{routesClass}}.home);
      if (next.errorMessage != null) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text(next.errorMessage!), backgroundColor: {{colorsClass}}.error),
        );
      }
    });

    return Scaffold(
      backgroundColor: {{colorsClass}}.background,
      appBar: AppBar(backgroundColor: Colors.transparent, elevation: 0,
          leading: const BackButton(color: {{colorsClass}}.textPrimary)),
      body: SafeArea(
        child: SingleChildScrollView(
          padding: const EdgeInsets.all({{dimensionsClass}}.lg),
          child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
            Text({{stringsClass}}.registerTitle, style: {{textStylesClass}}.heading1),
            const SizedBox(height: {{dimensionsClass}}.xs),
            Text({{stringsClass}}.registerSubtitle,
                style: {{textStylesClass}}.bodyMedium.copyWith(color: {{colorsClass}}.textSecondary)),
            const SizedBox(height: {{dimensionsClass}}.xxl),
            TextInput(label: {{stringsClass}}.registerName, onChanged: notifier.nameChanged),
            const SizedBox(height: {{dimensionsClass}}.md),
            TextInput(label: {{stringsClass}}.registerEmail,
                keyboardType: TextInputType.emailAddress, onChanged: notifier.emailChanged),
            const SizedBox(height: {{dimensionsClass}}.md),
            TextInput(label: {{stringsClass}}.registerPassword,
                isPassword: true, onChanged: notifier.passwordChanged),
            const SizedBox(height: {{dimensionsClass}}.md),
            TextInput(label: {{stringsClass}}.registerConfirm,
                isPassword: true, onChanged: notifier.confirmPasswordChanged),
            const SizedBox(height: {{dimensionsClass}}.xl),
            PrimaryButton(label: {{stringsClass}}.registerButton,
                isLoading: state.isLoading, onPressed: notifier.submit),
            const SizedBox(height: {{dimensionsClass}}.xl),
            Row(mainAxisAlignment: MainAxisAlignment.center, children: [
              Text({{stringsClass}}.registerHaveAccount, style: {{textStylesClass}}.bodyMedium),
              GestureDetector(
                onTap: () => Navigator.pop(context),
                child: Text({{stringsClass}}.registerSignIn,
                    style: {{textStylesClass}}.bodyMedium.copyWith(
                        color: {{colorsClass}}.primary, fontWeight: FontWeight.w600)),
              ),
            ]),
          ]),
        ),
      ),
    );
  }
}
